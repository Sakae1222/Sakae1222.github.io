---
tags: Programming Python
---

## FreeCodeCamp Python Certification - Loops and sequences Review

### [Loops and sequences review](https://www.freecodecamp.org/learn/python-v9/review-loops-and-sequences/review-loops-and-sequences)

**Accessing Elements in a List:**

```
cities = ['Los Angeles', 'London', 'Tokyo']
cities[0] # Los Angeles
```

**Creating Lists Using the `list()` constructor:**

```
developer = 'Jessica'

print(list(developer)) 
# Result: ['J', 'e', 's', 's', 'i', 'c', 'a']
```

**List Mutability:**

```
programming_languages = ['Python', 'Java', 'C++', 'Rust']
programming_languages[0] = 'JavaScript'
print(programming_languages) # ['JavaScript', 'Java', 'C++', 'Rust']
```

**`append()`:**

```
numbers = [1, 2, 3, 4, 5]
numbers.append(6)
print(numbers) # [1, 2, 3, 4, 5, 6]

## Appending lists
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.append(even_numbers)
print(numbers) # [1, 2, 3, 4, 5, [6, 8, 10]]
```

**`extend():`**

```
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.extend(even_numbers)
print(numbers) # [1, 2, 3, 4, 5, 6, 8, 10]
```

The `range()` function: Used to generate a sequence of integers.
